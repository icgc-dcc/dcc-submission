<configuration debug="false">
  <property name="log.dir" value="/srv/webapp/dcc/server/logs" />
  <property name="log.name" value="dcc-server" />
  <property name="pattern" value="%date{ISO8601} [%thread] %-5level %logger{20} - %msg%n" />

  <contextListener class="ch.qos.logback.classic.jul.LevelChangePropagator">
    <resetJUL>true</resetJUL>
  </contextListener>

  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>${pattern}</pattern>
    </encoder>
  </appender>

  <!-- 
  <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <File>${log.dir}/${log.name}.log</File>
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <FileNamePattern>${log.dir}/${log.name}.%d{yyyy-MM-dd}.log</FileNamePattern>
    </rollingPolicy>
    <encoder>
      <pattern>${pattern}</pattern>
    </encoder>
  </appender>
  -->
  
  <root level="info">
    <appender-ref ref="STDOUT" />
    <!-- 
    TODO: Enable when we can redirect console to /srv/webapp/dcc/server/logs/console.log
    Also need to consider if logging to a file at the above location will work with MapReduce.
    We may need to do something special to disable file appender with cascading.
    <appender-ref ref="FILE" />
     -->
  </root>
</configuration>