{{#if next}}
<div class="alert alert-success"><button class="close" data-dismiss="alert">×</button>This release has been closed. The current open release is <a href="/releases/{{next}}">{{next}}</a>.</div>
{{/if}}

<div class="row-fluid page-header">
  <ul class="breadcrumb">
    <li>
      <a href="/releases">Releases</a> <span class="divider">/</span>
    </li>
  </ul>

  <div class="pull-left">
    <h1><small>RELEASE</small> {{name}}<br>
    <small class="{{lowercase state}}">{{state}}</small>
    {{#if_completed state}}
    <small> on {{date releaseDate}}</small>
    {{/if_completed}}
    </h1>
  </div>
  <div class="pull-right">
    {{#if_opened state}}
    <button id="complete-release-popup-button" class="btn btn-success">Complete</button>
    {{/if_opened}}
  </div>
</div>
<div class="row-fluid">
  <div class="span6">
    <ul class="nav nav-list">
      <li class="nav-header">Submission Summary</li>
      <table class="table summary">
        <tbody>
        {{#if summary.SIGNED_OFF}}
        <tr class="signed">
          <td width="200">Signed Off</td>
          <td>{{summary.SIGNED_OFF}}</td>
        </tr>
        {{/if}}
        {{#if summary.VALID}}
        <tr class="valid">
          <td width="200">Valid</td>
          <td>{{summary.VALID}}</td>
        </tr>
        {{/if}}
        {{#if summary.QUEUED}}
        <tr class="queued">
          <td width="200">Queued</td>
          <td>{{summary.QUEUED}}</td>
        </tr>
        {{/if}}
        {{#if summary.INVALID}}
        <tr class="invalid">
          <td width="200">Invalid</td>
          <td>{{summary.INVALID}}</td>
        </tr>
        {{/if}}
        {{#if summary.NOT_VALIDATED}}
        <tr>
          <td width="200">Not Validated</td>
          <td>{{summary.NOT_VALIDATED}}</td>
        </tr>
        {{/if}}
        </tbody>
      </table>
    </ul>
  </div>
</div>

<ul class="nav nav-tabs">
  <li class="active"><a href="#files" data-toggle="tab">Submissions</a></li>
</ul>
<div class="tab-content">
  <table id="submissions-table" width="100%" class="submissions table table-striped"></table>
</div>