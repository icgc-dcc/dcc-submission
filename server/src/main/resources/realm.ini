[users]
admin = adminspasswd, admin
brett = brettspasswd, curator
richard = richardspasswd, submitter, guest

[roles]
admin = *
curator = release:view, release:close
submitter = release:view, release:signoff
guest = release:view

