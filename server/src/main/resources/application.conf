http {
  listen: localhost
  port: 5379
  ws {
    path: "/ws"
  }
}
mongo {
  uri: "mongodb://localhost/icgc"
}
shiro {
  realm: "realm.ini"
}
fs {
  root : "/tmp/my_root_fs_dir" # local for now (FIXME)
  use_hdfs : false # HOCON is smart enough to map it to a boolean
  hdfs { # TBD
    host: localhost # surround with double-quotes for IP addresses
    port: 9000
  }
  local { # TBD
    key1: value1
    key2: value2
  }
}