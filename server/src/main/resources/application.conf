http {
  listen: "0.0.0.0"
  port: 5380
  ws {
    path: "/ws"
  }
  resources: [
    "../client/target/main/webapp/"
  ]
}
mongo {
  uri: "mongodb://hcn81.res.oicr.on.ca/icgc"
}
shiro {
  realm: "realm.ini"
}
fs {
  # Root directory of DCC filesystem
  root : "/tmp/dcc_root_dir"
  # FileSystem Scheme. This sets hadoop's fs.default.name
  url: "hdfs://hboot1.res.oicr.on.ca"
  # url : "file:///" 
}
sftp {
  port : 5322
  path : "~/conf/sshd.pem"
}
