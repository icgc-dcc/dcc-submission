---
# Copyright 2016(c) The Ontario Institute for Cancer Research.

# Role dependencies of the server which specify application specific role parameterization
dependencies:

   # Server currently requires Java 6
  - { role: java, java_name: 'oracle-java6' }
  
  # Linked with application configuration
  - { role: reference, target_dir: "{{ data_dir }}" }
